export type MsgT = {
  channel: channel,
  oraculo: oraculo,
  join: join,
  leave: leave
}

export type channel = string
export type oraculo = string
export type join = string
export type leave = string